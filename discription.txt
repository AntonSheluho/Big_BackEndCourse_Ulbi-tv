npm install bcryptjs  => хеширования паролей

npm i express-validator  => Валидатор
const hashPassword = bcrypt.hashSync(password, 7)
const validPassword = bcrypt.compareSync(password, user.password);

npm install jsonwebtoken  =>  Генерирует токен